role: Client
topology: local-deployment.example.yaml
experiment: paper_pid_lan.example.yaml
tls:
  ca: ../.tmp/lan-certs/ca.pem
  certificate: ../.tmp/lan-certs/client.pem
  private_key: ../.tmp/lan-certs/client.key
timeouts:
  startup: 30
  step: 30
  idle: 30
  shutdown: 5
